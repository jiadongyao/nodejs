let array = ['x', 'y', 'z'];

for (let index in array) {
    // console.log(index);
    // console.log(array[index]);
}

for (let v of array) {
    console.log(v);
}
// Ctrl + Shift + F10 run current file
// Alt + F10 run last file
// Shift + Alt + F10 run selected file